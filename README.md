# URL path join

Join URL path segments without duplicate slashes or accidental empty pieces.

TypeScript Urljoin uses only the typescript standard library; there is no service or dependency to install.

```
urljoin.ts
```

Run the TypeScript Urljoin test next to the implementation for concrete examples.